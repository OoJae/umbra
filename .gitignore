# secrets — never commit
.env
.env.local
.env.*.local

# node
node_modules/
web/.next/
web/out/

# foundry
contracts/out/
contracts/cache/
contracts/broadcast/

# python
**/__pycache__/
*.py[cod]
.venv/
engine/.venv/
.pytest_cache/
.ruff_cache/

# misc
.DS_Store
*.log

# playwright-mcp scratch
.playwright-mcp/

# Internal planning artifacts. Kept out of the public repo: the strategy doc analyses the
# judges by name and reasons about the strength of the competing field, which has no place in a
# repository we invite those judges to read.
docs/flare-summer-signal-win-strategy.md
docs/claude-code-master-prompt.md

# Playwright / design-review screenshots
*.png
.playwright-mcp/

# Demo film: 86MB of captured plates and staged assets stay local.
# The text artifacts (script, brief, storyboard, frame spec) are tracked.
demo/shots/
demo/videos/*/assets/
demo/videos/*/capture/
demo/videos/*/renders/
demo/videos/*/snapshots/
demo/videos/*/node_modules/

demo/videos/*/.thumbnails/
demo/videos/*/.hyperframes/frame-packets/

demo/videos/*/snapshots/
demo/videos/*/assets/voice/

# Frame-worker verification harnesses (transient)
demo/videos/*/__*.html
demo/videos/*/__*.js

demo/logo/*.png binary asset is small and IS tracked (it is a deliverable)
!demo/logo/*.png

# ── Internal working files. Kept locally, out of the public repo. ──────────────
# This repo is the front door for hackathon judges. Build logs, agent standing
# orders, the planning spec and the whole video-production tree are working
# material, not the product — and the raw voiceover is a personal recording.
CLAUDE.md
PROGRESS.md
.gcp-env
docs/umbra-build-guide.md
docs/video-shot-list.md
demo/
